# Decisions - 2026-06

## 2026-06-15: Adopt layered RepoMind OS migration

Status: accepted.

Decision maker: user.

Context:

The repository already had an older AI collaboration system under `docs/`, including role prompts, Codex workflow rules, document ownership rules, and tool or Skill policies.

RepoMind OS was introduced afterward under `.ai-governance/`.

Decision:

Use layered migration.

Consequences:

- `.ai-governance/` becomes the highest governance layer for the repository.
- Existing `docs/*` AI collaboration documents remain as historical governance documents and import evidence.
- Old role prompts are not automatically converted into RepoMind OS role files.
- A full role library should not be created immediately.
- Verified project state should first be written into `.ai-governance/PROJECT_STATE.md`.
- Current bootstrap status should be written into `.ai-governance/handoff/CURRENT.md`.
- Old foundation documents should be synchronized only after user-approved scope is clear.

Not decided yet:

- Whether old roles should be migrated into `.ai-governance/roles/`.
- Whether V0 should be marked complete or code-complete candidate pending manual verification.
- Whether the next product direction should be V0 acceptance/release polish or V1 planning.

Stop boundaries:

- Do not write implementation code as part of this decision.
- Do not ask Codex to modify source files during bootstrap.
- Do not start V1 implementation from this decision alone.
- Do not introduce cloud development, barcode, OCR, AI, reminders, new dependencies, or new Skills without separate user approval.

## 2026-06-15: Add V0.1 food search enhancement before V1

Status: accepted.

Decision maker: user.

Context:

The user reported that V0 manual acceptance checks all passed. During acceptance, the user also identified that the current app has no food search feature.

Decision:

Do not move directly into V1 yet. Add a V0.1 local food search enhancement first.

Consequences:

- V0 core local workflow is accepted by the user.
- The next product task should be V0.1 food search enhancement.
- V1 cloud data and reminder planning is deferred until the search follow-up is handled or explicitly deferred.
- Search must remain within V0 local data boundaries unless the user approves otherwise.

Initial V0.1 boundary:

- Add home-list search.
- Search at minimum by food name.
- Keep search local.
- Combine search with existing status and category filters.
- Preserve `expiryDate` sorting.
- Show a clear empty result message.

Out of scope for this decision:

- Cloud database.
- Cloud functions.
- User account or openid isolation.
- Multi-device sync.
- Barcode scanning.
- OCR.
- AI extraction or AI search.
- Reminders or subscription messages.
- New dependencies, tools, or Skills.

## 2026-06-15: Sync latest RepoMind OS upstream bootstrap updates

Status: accepted.

Decision maker: user.

Context:

The user asked to inspect the latest updates in `2w2w23e/RepoMind-OS` and merge the relevant updates into this project.

The upstream RepoMind OS README describes RepoMind OS as a flexible AI governance starter / bootstrap system rather than a fixed multi-role framework. It also documents a quickstart flow and emphasizes bounded Codex prompts with allowed files, forbidden files, validation, commit rules, and stop conditions.

Decision:

Merge the relevant upstream bootstrap updates into this repository without changing mini program runtime code.

Merged updates:

- Add the upstream `Repository-Read Rule For Every Role Response` to `.ai-governance/BOOT.md`.
- Add `docs/quickstart.md` as the project-specific RepoMind OS quickstart because `.ai-governance/START_HERE.md` points users to `docs/quickstart.md`.
- Update `README.md` to link the quickstart and reflect the current V0 / V0.1 status.

Consequences:

- Future role answers must refresh the minimum required repository context before substantive recommendations.
- The quickstart link in `START_HERE.md` now resolves to an existing project file.
- The repository remains within V0.1 planning boundaries; no implementation code was changed.

Out of scope:

- Do not replace product README with the upstream RepoMind OS README.
- Do not overwrite project-specific `.ai-governance/PROJECT_STATE.md`, `PROJECT_INTAKE.md`, `handoff/CURRENT.md`, or decisions with template content.
- Do not introduce new roles beyond already present approved/bootstrap roles.
- Do not change mini program runtime code.

## 2026-06-16: Expand RepoMind OS upstream kernel sync

Status: accepted.

Decision maker: user.

Context:

The user again asked to inspect `2w2w23e/RepoMind-OS` and merge the latest updates into this project.

Upstream template updates included a stronger repository-read rule, a Context Refresh Header, a Coordination Graph state machine, a Role Integration Protocol for existing roles, stronger first-window foundation gates, and Project Governor role updates.

Decision:

Merge the relevant upstream RepoMind OS kernel updates into this project while preserving project-specific state.

Merged updates:

- `.ai-governance/BOOT.md`: updated repository-read rule, Context Refresh Header, Coordination Graph routing, and Role Integration routing.
- `.ai-governance/CONTEXT_INDEX.md`: added first-window bootstrap route, Coordination Graph route, Role Integration route, wildcard-route safety rule, and failure/repeated-problem route.
- `.ai-governance/COORDINATION_GRAPH.md`: added coordination graph state machine and execution eligibility rules.
- `.ai-governance/ROLE_INTEGRATION_PROTOCOL.md`: added existing-role integration protocol.
- `.ai-governance/FIRST_WINDOW_PROTOCOL.md`: added bootstrap completion gate, foundation-before-execution gate, draft-before-write rule, and role integration gate.
- `.ai-governance/roles/PROJECT_GOVERNOR.md`: updated Project Governor read order, memory lookup, context refresh requirement, role alignment review, and Codex boundary.

Consequences:

- Future substantive answers should include a Context Refresh Header.
- Existing roles and old project prompts must be classified through Role Integration before they are activated as RepoMind OS roles.
- Execution routing should check `COORDINATION_GRAPH.md` and reach `EXECUTION_ALLOWED` only after the task boundary and approval are explicit.
- Project-specific state files were preserved rather than overwritten from the template.

Out of scope:

- No mini program runtime code was changed.
- No V1 implementation was started.
- No new product feature was implemented.
- No old role prompt was automatically converted into an active RepoMind OS role.
